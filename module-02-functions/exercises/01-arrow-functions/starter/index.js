/** @param {number} n */
export const double = (n) => {
  void n;
};

/**
 * @param {string} name
 * @param {string} [greeting]
 */
export const greet = (name, greeting) => {
  void name;
  void greeting;
};

/**
 * @param {string} zone
 * @param {number} level
 */
export const buildAlert = (zone, level) => {
  void zone;
  void level;
};

/**
 * @param {string} tag
 * @param {...string} names
 */
export const tagAll = (tag, ...names) => {
  void tag;
  void names;
  return [];
};
